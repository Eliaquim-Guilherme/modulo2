//valores          2 5 1 6 1
//indice/posição - 0 1 2 3 4
/*let frutos = ["maça", "banana", "acabaxi"]
let vazio = []
 vazio[1] = 5 // adiciona valor na posição do indice indicado
 vazio[0] = 30
 vazio.push(50) //push adiciona novo valor no fim do vetor
 vazio.pop() //exclui o ultimo valor
 vazio.unshift() //adiciona no inicio do vetor
console.log(vazio)*/

let numeros = [2, 5, 1, 6, 1]
// console.log(numeros.length) //indica a quantidade de unidades do vetor 

// for(let pos = 0; pos < numeros.length; pos++){
//     //console.log("A posição " + pos + " tem valor: " + numeros[pos])
//     console.log(`A posição ${pos} tem o valor ${numeros[pos]}`)
// }

//  numeros.forEach(function(numeros){
//     console.log(numeros)
//  })

//for(let pos in numeros){
    //console.log(numeros[pos])
//}